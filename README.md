# Task_Tracker

- Старт проекта 16.11.2025

## Вводная часть проекта

1. Создание проекта на GitHub

2. Завершение проекта, загрузка его на GitHub.

3. Завершение создания MD файла и закрепление ссылки проекта [roadmap.sh](https://roadmap.sh/projects/task-tracker)

4. Завершение проекта на `roadmap.sh`

## Постановка задачи по разработке `Трекер задач`

**Ожидаемы результат:**

Создаnm CLI-приложение для отслеживания задач и управления списком дел. По возможности прикрутить интерфейс на одностраничном сайте HTML и базовые стили (CSS)

*Трекер задач* — это проект, предназначенный для отслеживания и управления задачами. В этом проекте: простой интерфейс командной строки (CLI) для отслеживания того, что вам `нужно сделать`, что вы `уже сделали` и `над чем работаете` в данный момент. Этот проект поможет мне отработать навыки программирования, включая работу с файловой системой, обработку пользовательского ввода и создание простого CLI-приложения (и возможно  front-end).

## Требования

Приложение должно запускаться из командной строки, принимать действия и вводимые пользователем данные в качестве аргументов и сохранять задачи в JSON-файле.

### Пользователь должен иметь возможность

- Добавление, обновление и удаление задач

- Отметить задачу как выполняемую или выполненную

- Список всех задач

- Перечислите все выполненные задачи.

- Перечислите все задачи, которые не выполнены

- Перечислите все задачи, которые находятся в процессе выполнения.

### Ограничения, которые следует учитывать при реализации

- Для создания этого проекта вы можете использовать любой язык программирования.

- Используйте позиционные аргументы в командной строке для принятия ввода пользователя.

- Используйте JSON-файл для хранения задач в текущем каталоге.

- Если файл JSON не существует, его следует создать.

- Используйте собственный модуль файловой системы вашего языка программирования - для взаимодействия с JSON-файлом.

- Не используйте внешние библиотеки или фреймворки для сборки этого проекта.

- Обязательно корректно обрабатывайте ошибки и пограничные случаи.

### Список команд и их использование

Приложение использует флаги для выполнения команд.

```bash
# Добавить новую задачу
go run . -add="Купить Bitcoin"

# Редактировать задачу с ID 0
go run . -edit="0:Купил Bitcoin по самой низкой цене"

# Переключить статус задачи с ID 1 (выполнена/не выполнена)
go run . -toggle=1

# Удалить задачу с ID 2
go run . -del=2

# Вывести список всех задач (также выполняется по умолчанию, если нет других флагов)
go run . -list
```

### Свойства задачи

- `id`: Уникальный идентификатор задачи

- `description`: Краткое описание задачи

- `status`: Статус задачи ( todo, in-progress, done)

- `createdAt`: Дата и время создания задачи.

- `updatedAt`: Дата и время последнего обновления задачи.

Добавить эти свойства в JSON файл, добавление свойств при добавлении новой задачи и обновление их при обновлении задачи.

## Короткая инструкция для меня

### Настройка среды разрабоктки

- Язык програмирования Go lang, по возможности реализовать фронт на HTML + CSS
- Разработка в IDE VS Code

### Инициализация проекта

- Создание нового каталога `task_tracker`
- Инициализация контроля версий (Git), загрузка проекта в новый репозиторий на GitHub

### Реализация функций

- Создание базовой структуры CLI для обработки пользовательского ввода
- Реализовать каждую функцию последовательно, тестируя перед переходом к следующей

### Тестирование и отладка

- Протестировать каждую функцию по отдельности, чтобы убедиться, что они работают как надо
- Проверить JSON файл и убедиться, что задачи сохраняются корректно
- Устранять баги и неполадки, возникающие в процессе разработки

### Завершение разработки

- Убедиться, что все функции реализованы и протестированы
- Очистить код и добавить комментарии, где это необходимо
- Написать и поправить README, о том как использовать трекер задач, первоначальный README сохранить в папке проекта отдельно (дял истории)

```bash



### Дальнейшие шаги

Сейчас программа переработана на базе библиотеки 'go-pretty'
Целевое ТЗ необходимо реализовать на внутренних библиотеках, но было добавлена поскольку, это функционал добавляющий некий интерфейс и удобство (возможно в будущем пересмотрю и сделаю как было в начале задачи и уберу библиотеку)



### Резюме и дальнейшие шаги

1.  **Привел README в соответствие с реальностью.** 
2.  **Пересмотреть ограничение по библиотекам.** Использование `go-pretty` — это нормально. Возможно, стоит убрать этот пункт из ограничений, так как он усложняет разработку без веской причины.
3.  **Решить, что делать дальше.** 
Есть выбор:
    *   Оставить все как есть (с флагами).
    *   Начать рефакторинг кода, чтобы реализовать систему с подкомандами (`add`, `list` и т.д.)


- Как мне переделать парсинг аргументов, чтобы использовать позиционные аргументы (подкоманды `add`, `list`), как в README.md?
- Как реализовать статусы "todo", "in-progress", "done" вместо простого boolean "Completed"?
